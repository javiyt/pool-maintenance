{
  "name": "pool-maintenance",
  "version": "2.0.0",
  "private": true,
  "description": "Water chemistry tracker for pool owners — save measurements, check water quality, get chemical recommendations.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^8.1.4",
    "vitest": "^4.1.10"
  }
}
